/**
 * Canonical production URL used for sitemap, JSON-LD, robots.txt, etc.
 * Must match the `metadataBase` value in `src/app/layout.tsx`.
 */
export const BASE_URL = 'https://www.volunteerready.org';

export const FOUNDER_BOOKING_URL =
	'https://calendar.app.google/yvbL6zwB2imBKXms9';
